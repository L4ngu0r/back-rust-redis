[package]
name = "back_redis"
version = "1.0.0"
edition = "2018"

[dependencies]
tokio = { version = "0.2", features = ["macros"] }
warp = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
redis = "0.15.1"
